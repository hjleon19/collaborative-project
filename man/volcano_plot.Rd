% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_functions.R
\name{volcano_plot}
\alias{volcano_plot}
\title{Volcano Plot}
\usage{
volcano_plot(
  data,
  logFC,
  pval,
  alpha = 0.05,
  lfc_thresh = 1,
  title = "Volcano Plot",
  already_adjusted = FALSE
)
}
\arguments{
\item{data}{Data frame containing logFC and p-value columns}

\item{logFC}{Column name for log fold change values}

\item{pval}{Column name for p-values}

\item{alpha}{FDR significance threshold (default = 0.05)}

\item{lfc_thresh}{Log fold change threshold (default = 1)}

\item{title}{Plot title}

\item{already_adjusted}{Logical. TRUE if p-values are already FDR adjusted (default = FALSE)}
}
\value{
A ggplot2 object
}
\description{
Creates a volcano plot from differential expression results.
  Significant genes are coloured red (up) or blue (down). The y-axis shows
  -log10(FDR-adjusted p-value) so the most significant genes appear at the top.
}
\examples{
volcano_plot(deg, logFC = "logFC", pval = "padj", already_adjusted = TRUE)
}
